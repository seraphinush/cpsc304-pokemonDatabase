CREATE TABLE Species (
name 			CHAR(10),
rarity 			INTEGER,
description 	CHAR(255), 
minHeight 		DECIMAL(18,2),
maxHeight 		DECIMAL(18,2),
minWeight 		DECIMAL(18,2),
maxWeight 		DECIMAL(18,2),
PRIMARY KEY (name));
